apiVersion: v1
kind: Service
metadata:
  name: scanner-app-service
  namespace: phishing-url-scanner
spec:
  selector:
    app: scanner-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP